[package]
name = "nepospot-generate"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0"
chrono = "0.4"
mediawiki = "0.2"
tokio = { version = "1", features = ["full"] }
